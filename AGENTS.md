# Agent Instructions for DeepInfant

## Python Package Management
- Use `uv` instead of `pip`.
- Install dependencies with `uv add ...`.

## Running Python
- Use `uv run ...` instead of calling `python` directly.
- Example: `uv run app.py`
- Example: `uv run train.py`
